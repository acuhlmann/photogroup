// Browser stub for load-ip-set (Node.js-only module)
// WebTorrent uses this but it's not needed in browser mode
export default function loadIPSet() {
  // Empty stub - load-ip-set is not used in browser
  return Promise.resolve(null);
}


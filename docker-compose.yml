version: '3.8'

services:
  photogroup:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: photogroup-app
    ports:
      - "127.0.0.1:${PORT:-8081}:8081"
      - "127.0.0.1:${WS_PORT:-9000}:9000"
    environment:
      - NODE_ENV=production
      - PORT=8081
      - WS_PORT=9000
      - WS_HOST=0.0.0.0
      # Optional: Twilio credentials for ICE servers
      # - TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID}
      # - TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8081/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - photogroup-network

networks:
  photogroup-network:
    driver: bridge

